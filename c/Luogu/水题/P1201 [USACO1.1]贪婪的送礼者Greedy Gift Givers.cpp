#include<string>
#include<cstdio>
using namespace std;
struct aa
{
	int m,tp;
	string name;
	string to[10];
}p[10];
int main()
{
	int n;
	scanf("%d",&n);
	for(int i=0;i<n;i++)
		scanf("%s",&p[i].name);
	while(1)
	{
		scanf("%d %d",&p[i].m,&p[i].tp);
	}
}
